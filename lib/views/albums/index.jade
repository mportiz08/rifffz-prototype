#library.container
  .row
    .span16
      ul.media-grid
        each album in albums
          li(data-artist=album.artist, data-album=album.name)
            a(href=album.path)
              img.thumbnail(src=album.cover, width="210", height="210")
